# Update 62

**PROCESSED IN THE CLOUD, NOT ON THE DROPLET.** Every run in this log was in
a Claude Code cloud session (a fresh container: tcg, no KVM, 4 cores, 15 GB),
2026-09-25, and later Updates will be processed the same way. So the
droplet's `~/codexzig`, `~/codexir` and `~/zigemit` were NOT refreshed at
U62: they are still U61's, and nothing on the droplet is U62. The U62
bundles named below existed only in that container, were built from
3eac1674 that day, and are gone with it. Regenerate; do not look for them.
Times here are the container's and came out in the droplet's ballpark.

    3eac1674  Update 62   seed CC7DD4559232C558
    3af67f99  Update 61   seed 7BCD5BC6BCE0AF41

One squashed commit over U61: 660 files, +74,214 / -11,107, roughly six times
U61's size. The bulk is codex/test (223 files), codex/plugs (116) and
codex/compiler (31). The seed name is the sha256 prefix of `seed/Codex.cdx`,
checked for both seeds (U61's matches U61.log); the release notes' seed
`CC7DD455` agrees. Release notes: `docs/PM/Active/GitHubUpdates/GitHubUpdate62.md`.

Upstream has no tags or GitHub releases; an Update is a commit on `master`.
The checkout every arm built from was a depth-1 clone of upstream `master`
at `~/showell_repos/cobblestone-u62rel` (the name kept for the receipts'
sake), not a worktree.

## What upstream did with ours

The release notes say they took issue 153 with PR 154, PRs 155 and 156, and
issue 157. `u62-candidate` is U61 + PRs 154-156 (U61.log), and all three PR
heads sit directly on U61, so measuring each PR head against U62 file by file
covers the 8 files the candidate touches:

     6 byte-identical   empty-list-as-{integer,text}.{codex,failing} (PR 154),
                        act-unused-bind.{codex,expected} (PR 156)
     0 prose only
     1 code as sent     ZigEmitter.codex: both PRs present, file otherwise moved
     1 dropped          compiler-backlog.md: PR 154's COMPILER-85 row

No prose was edited this time, unlike U61. The emitter diffs of PRs 155 and 156
carry no comments (one table row; two changed lines in each of the two `act`
emitters), so "as sent" there is code only. The one prose change we sent, PR
154's backlog row, was dropped whole rather than reworded.

- **PR 155 (`bit-not`)**: our table row is byte-identical at ZigEmitter:1103.
  The patch no longer reverse-applies only because its context moved:
  `list-length` no longer follows `bit-shr` in the table.
- **PR 156 (unused `act` bind)**: both hunks reverse-apply cleanly on U62's
  ZigEmitter, offset -44 lines. Landed verbatim.
- **PR 154's backlog row**: not carried. Our row filed the empty-list hole as
  COMPILER-85; upstream fixed it outright (main 26013) and gave COMPILER-85 to
  the issue's SECOND shape instead: desugared tuples, revised records and
  derived `show` names losing their instantiated IR types (main 26027). Neither
  number names the empty-list defect in U62's backlog. The two refusal tests
  are theirs now, unchanged.
- **Issue 157**: all three items acted on, none measured here yet.
  1. The literal table rebuilt at every reference is COMPILER-86: the
     reproducer went from 528 bytes per read to 0 (main 26048); the compiler
     shares 32 of its 41 tables, the Zig plug shares constant tables (main
     26148), record fields and a left `&` operand are covered (main 26370).
     Three tables remain unshared, named in
     `docs/Designs/Active/Compiler/ConstantSharing.md`. COMPILER-86 is still
     open in the backlog for the non-literal case.
  2. Zig plug allocations no longer bypass std's `0xAA` poisoning (main 26044).
  3. The plug refuses `ReleaseFast` and `ReleaseSmall` and benches at
     `ReleaseSafe` (main 26086). Ours that this breaks:
     `outbound/evidence-raytracer-nearest-first/{bench2,verify-bench}.sh`
     both run ReleaseFast. They are sent evidence, so a record, not a fix.

ZigEmitter.codex as a whole moved +157 / -60 over U61; PRs 155 and 156 are a
few lines of that. The rest (issue 157 items 1-3 and whatever else) is
unread.

The notes credit "the fixes and the initial measurements" to us, "developed
with Claude", and claim the regression fixtures and the release proof as
theirs.

Ours to follow, from the notes alone and not yet run:

- Frontend changes the Rust arm will have to meet. Checked method templates
  and per-demand dictionary slots (COMPILER-90). A constrained function over
  a class with ONE instance is now specialized to it, so a call at a type
  with no instance is a type error (main 27104). `[] == []` needed its own
  release-time fix (main 27130). And the empty literal is list-typed.

## cobblestone-qemu: GREEN 5/5 in 8m58s, after four reds that were all ours

    ~/runs/20260925T110102Z-u62             checkers b0f9312
    ring plug 23s · fib 7s · zigemit 37s · codexir 3m29s · x86emit 4m13s
    verify   zig PASS · qemu PASS (IR identical to the native tool) · x86 PASS · boot PASS (6765)

Run in a cloud container (tcg, no KVM, 4 cores), not the droplet, so the
times are not comparable with U61's 12m43s.

Every red was the harness failing to keep up with how U62 changed upstream's
BUILD, not a defect in U62:

1. **Ring plug: `Undefined name: shared-const-names`.** The Zig plug calls
   IR/ConstShare (COMPILER-86); upstream's `plugs/zig/build.ps1` bundles it
   via a new `-CompilerChapters`. `bundle_ringplug.ps1` now reads that from
   the checkout (`9ab698a`).
2. **fib transpile REFUSED on a `@compileError`.** U62's plug emits a
   top-level comptime refusal of ReleaseFast/ReleaseSmall into every program:
   issue 157 item 3, as landed (main 26086). It is fixed prelude text, so it
   went into `prelude-comptime-guards.txt`, which had been empty since U54.
3. **codexir: nine CDX3002s.** The checker and X86_64Chapter call into the
   two new chapters without citing them. `bundle_ir_to_x86.ps1` kept its own
   list of 64 compiler chapters; it now reads `build/compiler-order.txt`
   (upstream's build refuses a chapter with no row there) and names only the
   ten it leaves out, each with its reason. Same lines on U62 as the hand list
   plus the two new chapters; only the order moved, to upstream's.
4. **codexir: IRTextMeta has no `method-templates`.** U62 moved IR emission
   into `prepare-method-ir` (lift, `eq-attach-helpers`, prune,
   `method-materialize`). Our harness was a copy of the driver, and reading
   the diff showed the copy had ALREADY drifted: it never attached the
   equality helpers the driver has attached since COMPILER-44. So a program
   using generic equality got different IR from native codexir than from the
   seed. fib uses none, so the qemu check's IR diff could not see it. The
   harness now CALLS `compile-frontend-ir` -> `prepare-method-ir` ->
   `emit-ir-chapter` with `-WithDriver` (`b0f9312`): U61's "next" item 7,
   which had proposed exactly this. Its first run found one
   more stale list: `-WithDriver` named six foreword chapters that
   `assemble_unit.ps1` has added itself since U60, so every type in them came
   out twice (CDX3001 x15). Removed.

Re-run with codexir's harness carrying the hosted deck prologue that
codex-zig-transpiler's harnesses put in front of the same driver calls
(`823c5b1`; `b0f9312` had none and had only run fib):

    ~/runs/20260925T112535Z-u62             checkers 823c5b1
    GREEN 5/5 in 8m47s; codexir 3m26s
    eq-generic-recursive through native codexir | zigemit: __eq_ helpers on
    the wire, MATCHES all 9 expected lines

The principle, now in README: when an Update makes us edit our own chapter
lists or harnesses, read how upstream changed its side first, and derive from
their files.

Ours to follow:

- The equality-helper drift, dated: `eq-attach-helpers` is on the driver's IR
  path from U56 (`6cd2ca1`) on and absent through U55; no cobblestone-qemu
  commit before `b0f9312` mentions it. So cobblestone-qemu's native codexir
  lacked the driver's `__eq_*` helpers for U56-U61. codex-zig-transpiler's
  harnesses had the same gap: measured below.
- `emit_harness.py`'s `frontend_source` still stands in for the driver in the
  x86emit harness. Same question, not yet asked.
- A stray `main` branch on cobblestone-qemu (my mistake, at `e2321e1`, equal
  to `master`) is Steve's to delete; deleting it was refused from here.

## codex-zig-transpiler: the fixed point HOLDS at U62

    fixed point   byte-identical, 3,156,931 bytes of zig (U61: 3,007,652)
    arith.codex   MATCHES all 9 expected lines
    309s cold     stage 4 guest 105s (peak 723 MB) · stage 5 guest 103s (1430 MB) · NATIVE transpile 66s
    oracles       codexir + codexcheck rebuilt (113s, no guest), both smokes pass

`f0acd43` (source) and `711a0bf` (`generated/` repinned). The same three
build fixes as cobblestone-qemu (ring plug `-CompilerChapters`, the guard,
`compiler-order.txt` for the one chapter list, leaving out only BootPaint and
EntryPoint), and both harnesses now take the driver's IR front door,
`compile-frontend-ir` -> `prepare-method-ir` -> `emit-ir-chapter`.

For CodexZigHarness that is more than tidiness. Pass 1 of the fixed point is
the seed in IR mode, which IS that front door; pass 2 used to reach the same
IR through `compile-frontend-cdx`'s `cdx-ir` and a copied prune. At U62 the
front door also runs `method-materialize`, so a neighbouring path would have
been one Update from breaking the fixed point for a reason that was never the
emitter's.

**The equality-helper gap, measured** on upstream's
`codex/test/eq-generic-recursive` (resolved with `resolve_unit.ps1`):

    U61 codexzig   1 __eq_ def (the desugarer's __eq_Named); every generic ==
                   dangles, and the plug emits @compileError("zig plug: no
                   emitter for __eq_Pair@Text"): the program does not build
    U62 codexzig   4 defs (__eq_Box_82Integer, __eq_Box_82Text,
                   __eq_Pair_82Text, __eq_Named); MATCHES all 9 expected lines

So the gap FAILED LOUD in codexzig, a refusal at zig build rather than a wrong
answer, which is why nothing banked a wrong result from it. The IR oracle is the
softer case: `~/codexir` at U56-U61 emitted IR with the dangling reference
and no error, so an IR diff against the seed would differ there. Whether any
curated or Rust-graded unit uses generic equality over a parameterised type is
the question for those arms at U62.

Correction to this log's earlier line: U61 did not skip this repo (U61.log
has "the fixed point HOLDS at U61"). I had read only U61.log's head and tail.

Setup quirks from a cloud session: `build.py` wants `zig` on PATH (not a
path like cobblestone-qemu's `ZIG`) and pwsh at `~/.local/pwsh/pwsh`.

## The bundles, built in the container (not refreshed on the droplet)

    ~/codexzig   codexzig 9682f8d5, the fixed point's stage-7 binary (transpiler 711a0bf)
    ~/codexir    codexir 1ecb3797, codexcheck 69e51708 (build_codexir.py, both smokes pass)
    ~/zigemit    zigemit e9ad998c, cobblestone-qemu sandbox 20260925T112535Z-u62 (823c5b1)
    all          checkout /root/showell_repos/cobblestone-u62rel 3eac167; SHA256SUMS beside each receipt

Assembled by hand as at U60/U61: executable(s) + receipt + SHA256SUMS. The
curated arms' `common.provenance` reads all three as 3eac167.

## Curated at U62: run-zig 74 of 74; two re-freezes, both COMPILER-85

    run-zig              cobblestone 28 of 28 · roc 46 of 46
    freeze-upstream-ir   cobblestone 28 unchanged · roc 44 unchanged, 2 frozen
    ir-diff              cobblestone 28 same · roc 44 same, 2 differ

The two that moved are roc-tuple-two-lists and roc-tuple-same-list-twice:
`MkTup2`'s instantiated type is now `(list int-default)` where U61 closed it to
`int-default`, though its arguments are lists. That was issue 153's second
shape (posted as comment 5778393190 at U61); upstream fixed it as COMPILER-85
(main 26027). Reviewed token by token, nothing else moved; re-frozen as curated
`0f1bb41`. Note curated's default branch is `main`, not `master`.

ir-diff still says both differ, at the same token, which is `MkTup2`'s type
annotation where it is applied to `x` and `y`:

    rust        (name "MkTup2" (fn (tvar 2) (fn (tvar 3) (ctd "Tup2" (args (tvar 2) (tvar 3))))))
    U62 upstr.  (name "MkTup2" (fn (list int-default) (fn (list int-default) (ctd "Tup2" ...))))
    U61 upstr.  (fn int-default ...), the wrong instantiation COMPILER-85 fixed

Rust annotates the constructor reference with its GENERIC type (the
declaration's own variables); upstream with the type INSTANTIATED at the use.
Rust did not change and did not regress. It differed at U61 too, from a wrong
instantiation; it now differs from a right one. (An earlier draft of this
entry, and curated `0f1bb41`'s message, said the Rust frontend "has
COMPILER-85 to catch up on". That was wrong: COMPILER-85 was upstream
instantiating badly, and Rust never instantiates here.)

Whether Rust SHOULD instantiate constructor references is open: it matters
only if the Rust IR is meant to match upstream's token for token there. At U61
ir-zig (Rust IR -> zigemit -> zig, run) matched on these units, so the
uninstantiated variables break nothing downstream. Settle it with
rust-codex-compiler's own contract when that arm runs. `.rust.ir` is not
touched.

Nothing else moved, so none of the 74 units uses generic equality over a
parameterised type or a class method: the equality-helper and
method-template changes to the IR front door are invisible to this corpus.

## Curated, the Rust arms at U62: 74 of 74 on every arm

rust-codex-compiler `5c5704a`, `cargo +1.98 build --release` (1m03s) into
`~/build/rust-target`, where `tools.tsv` looks. The crate needs rustc 1.98;
the container shipped 1.94, so `rustup toolchain install 1.98` first.

    ir-rust      cobblestone 28 agree · roc 46 agree   (irdump == frozen .rust.ir)
    ir-zig       cobblestone 28 match · roc 46 match   (Rust IR -> U62 zigemit -> zig -> run)
    run-interp   cobblestone 28 match · roc 46 match   (codexrun)
    ir-interp    cobblestone 28 agree (38s) · roc 46 agree (60s)

ir-interp is codexrun hosting U62's own compiler, the new `prepare-method-ir`
front door included, against native codexir on the same bytes. It needs the
codexir bundle to ship `codexir-subject.codex` named by its receipt's
`ir-subject` line (`8f4eb102f2e49693`); the first cut of the bundle above left
it out.

ir-zig answers the tuple question in practice: the Rust IR's generic
`(tvar 2)` annotations on `MkTup2` build and run to the right output.

With run-zig and the re-freeze above, that is every curated arm except
run-wasm (codex-wasm-transpiler) on cobblestone and roc. The safari corpus
(safari-codex/units) is not run yet on any arm. rust-codex-compiler's own
gold-backed gates (`corpus_*_gate.sh`, `selfhost_gate.sh`; U61's "Rust gates")
are separate from these arms and not run yet.

## codex-wasm-transpiler at U62: both roads agree, the fixed point holds

    zig road      codexzig 9682f8d5 (codex-zig-transpiler 711a0bf), built at 3eac167
    both roads    61s; the two roads agree byte for byte; sample matches
    artifact      codexwasm.wat 8,713,721 bytes (U61 generation 1: 8,421,508)
    memory        1,659.1 MB, 40.5% of wasm32; ratchet +0.21% (not rebanked)

`3e01023` (source, build.py) and `0d804bc` (`generated/` repinned). This repo
had written the opposite of the drift rule into its bundler ("THIS LIST IS A
DELIBERATE COPY and the drift is accepted"), reasoning about coupling to a
SIBLING's bundler; `compiler-order.txt` is upstream's own file, so the list is
derived now like the other two, and the header says why. The harness takes
`compile-frontend-ir` -> `prepare-method-ir` and hands the prepared chapter
straight to `emit-wasm-chapter-stream`: no IR text, so the 945 MB round trip
it dropped stays dropped, and the memory moved +0.21%.

Its copied tail had drifted a THIRD way besides the equality helpers and
method-materialize: it read `wasm-exports` as a list of Text, a shape
upstream's emitter has refused since U59 for one `"|a|b|"` literal. For every
valid declaration it rooted no export, so exported functions were pruned.
Nothing in this repo declares one; upstream's `ir-roots-with-declared` is the
same reading done right, and the harness uses it now.

**A cloud-only quirk, fixed:** the zig road first reported codexzig's pin
UNVERIFIED. The receipt said `3eac167` and `borrowed_pin` wanted 8 to 40 hex
characters. git abbreviates to as few characters as are unique in the
repository, never fewer than 7; a full-history checkout gave 8
(`9fff850c` at U60), a depth-1 clone gives 7. Now 7 to 40.

## Curated run-wasm at U62: 74 of 74

    run-wasm     cobblestone 28 match · roc 46 match   (CODEXWASM=codex-wasm-transpiler checkout)

So every curated arm is green on cobblestone and roc at U62: run-zig,
ir-zig, ir-rust, run-interp, ir-interp, run-wasm, and the upstream re-freeze
(two reviewed, COMPILER-85). Not yet: the safari corpus on any arm, and
rust-codex-compiler's own gold-backed gates.

## Safari at U62: specs, every arm, and the whole app green

    specs        54 pass on codexrun, grading >= 2,351 values
    export       54 re-exported, 0 changed
    arms         run-zig, ir-zig, run-interp, run-wasm 54 match · ir-rust,
                 ir-interp 54 agree · freeze-upstream-ir 54 unchanged ·
                 ir-diff 54 same
    whole app    harness/build_wasm.sh: 2,430-command frame walks exactly,
                 1,200 frames of endurance, 300 painted frames

safari `7b58d93`, rust-codex-compiler `ef432ec`. Two things U62 moved under
safari, neither a defect in U62:

1. **The quire map moved.** U62 writes it in Codex
   (`codex/build/quiremapScript.codex`) and generates it to
   `build/host/windows/quire-map.ps1`; `build/quire-map.ps1` is a shim kept
   for the many scripts that dot-source it, and has no table. Our two TEXT
   readers of it broke: codexrun ("has no $QuireDirs table", every spec graded
   0) and safari's `cite_resolve.py` (IndexError). Both now read the
   generated file directly -- not the shim, and not the tool catalog the shim
   consults; Steve's call, since nothing of ours dot-sources and old
   checkouts are not kept. Both also read a quire added after the table by
   indexed assignment, which is how U62 registers `Accp`. The PowerShell
   callers (cobblestone-qemu `assemble_unit.ps1`, codex-zig-transpiler
   `resolve_unit.ps1`, rust `tools/r2_headers.ps1`) dot-source the shim and
   were unaffected. This repo's own `cite_resolve.py` and `check_bundles.py`
   parse it too; this repo is retired, so they are left broken.
2. **The browser module is ReleaseSafe now.** The plug's new comptime refusal
   of ReleaseFast/ReleaseSmall (issue 157 item 3) stopped `build_wasm.sh`,
   which built ReleaseSmall. Sizes: 730,157 bytes ReleaseSmall; 17,903,778
   ReleaseSafe; 2,645,676 ReleaseSafe `-fstrip`. ReleaseSmall had stripped
   debug info implicitly, so `-fstrip` restores that and the remaining 3.6x is
   the safety checks, which are what the issue asked for. Not yet eye-tested
   in a browser.

## rust-codex-compiler at U62: what grades meaning, and what U62 left to port

Steve's question: are the gold-backed gates load-bearing, when the goal is that
the Rust compiler MEANS what Codex means? Mostly not. The counter gate
(allocation order), the IR and selfhost gates (byte-identity ratchets, RED by
design) and the `$CODEX_GOLDS` truths (old declaration-layer dumps) measured
how closely the port reproduces upstream's INTERNALS. Deleted in `cf64f42`,
with the counter tooling and `safari/run.sh` (it ran safari's retired judge).
What grades meaning, and is the Rust verdict per Update from now on:

1. **Builtins ported** from `Types/Builtins.codex` (`ladder/builtins_probe.py`):
   U62 `+pit-input-hz +pit-count +cpu-park -uefi-read-file`.
2. **Corpus cut** by the port's own `bundle` (`tools/cut_units.py`, new;
   the ladder's `resolve_corpus.py` broke on the quire-map move):
   `~/units-3eac16746524`, 1,352 programs, 1,348 units.
3. **Meaning: `codexrun sweep`** over upstream's own tests with an
   `.expected`. It was not in the U60/U61 flow.
4. **Accept/refuse: `corpus_check_gate.sh` and `linear_gate.sh`.**

    sweep     743 of 1,081 print exactly what they should (68.7%)
    check     1,336 agree, 16 differ
    linear    25 agree, 0 differ, GREEN

**The sweep's 338 misses:** ~253 the interpreter cannot run (212 hardware/OS
builtins with no rule -- mmio, process-spawn, uefi keys -- 28 step limits,
effect handlers, `try`, `with-timeout`); 49 whose `opening` RETURNS its
answer, which codexrun never prints (docs/known-gaps.md; counted now); ~7
that print heap/allocation measurements; 2 an extra trailing blank line; and
**7 real semantic differences**:

    real-approx-modes, real-saturating   saturating overflow -> +inf, not the
                                         largest finite value (f32 and f64)
    real-approx-equality                 f32 tiny straddle approx-eq False, want True
    real-show-wide                       2^63 shows 9223372036854775808.0; U62
                                         (COMPILER-41) prints 9.22337203685478e+18
    unit-pattern-lit, unit-show          a Text unit: literal match falls through;
                                         show prints `Name abcd` for `abcd`
    variant-address                      empty-address False, want True

(66 more had been reported wrong only for a leading `\x01` or CRLF in the
`.expected`; the sweep normalises those now, as curated's arms do.)

**The check gate's 16:**

    WE REFUSE, CODEX ACCEPTS   method-template-contract-bounds-large/-small/
                               -demands, typeclass-instance-types: U62's checked
                               method templates (fresh method-local variables per
                               demand); we unify them and say CDX2001
    CODEX REFUSES, WE ACCEPT   empty-list-as-integer/-as-text (OUR issue 153;
                               the port has the same hole U62 closed upstream);
                               typeclass-method-local-fixed-binder (CDX2087,
                               U62's single-instance specialization),
                               typeclass-instance-mismatch (CDX2001),
                               typeclass-dict-by-hand (CDX2098);
                               paren-open-at-eof (CDX1000, U62's COMPILER-35)
    BOTH REFUSE, DIFFERENTLY   plus-plus-text (CDX2003 vs CDX1075),
                               vec-wide-refused (1 vs 2; U61's leftover)
    NOT SEMANTICS              missing-cite, unregistered-quire-cite (refuse to
                               bundle by design), method-template-producer-guards,
                               resolve-type-scope (cite the `Codex` quire, a
                               $QuireManifests entry our bundler does not read)

So U62's port work for Rust, beyond builtins: the 4 wrongful refusals and 6
missing refusals above, then the 7 sweep differences. None is done here.

## Verdict: Update 62 is verified. No defect found in U62 itself.

Every arm is at U62 and green on what it grades: bare metal 5/5, both
transpiler fixed points (zig; wasm on both roads), the curated corpora on
every arm (cobblestone 28, roc 46, safari 54), safari's specs and whole app.
What U62 took of ours it took faithfully: PRs 155 and 156 verbatim, PR 154's
tests byte-identical (its backlog row dropped as the fix made it moot), and
issue 153's two shapes and issue 157's three items all acted on, the second
shape of 153 visibly in the two roc re-freezes.

**Every red this cycle was ours, and almost all of it was drift.** Our
harnesses carried hand-kept copies of upstream's build: chapter lists, the
driver's IR tail, the quire table's location, the foreword chapters the
driver cites. U62 moved each of those and each copy broke, or had already
drifted silently: the equality helpers missing from the IR since U56, and
`wasm-exports` read in a shape upstream stopped accepting at U59. The bulk of
the work was replacing those copies with upstream's own sources:

    build/compiler-order.txt        chapter lists   (cobblestone-qemu,
                                                     codex-zig-transpiler,
                                                     codex-wasm-transpiler)
    plugs/zig/build.ps1             the ring plug's compiler chapters
    compile-frontend-ir +
    prepare-method-ir               every harness's IR (three repos)
    assemble_unit's cite resolver   the driver's foreword chapters
    build/host/windows/
    quire-map.ps1                   the quire table (Rust, safari)

Two upstream changes needed a decision rather than a sync, both our own asks
landing: the ReleaseFast/ReleaseSmall refusal (issue 157) made safari's
browser module ReleaseSafe (2.6 MB stripped, from 0.7 MB), and the guard
entered cobblestone-qemu's and the transpiler's precondition lists.

**What U62 leaves for us is Rust catch-up, not upstream defects:** the four
builtins (done), four programs the port refuses and six it accepts where U62
decides otherwise, and seven runtime differences in the sweep. Listed in the
rust-codex-compiler section above.

**Out to Damian from this cycle: nothing.**

Also new this cycle: processed entirely in the cloud (see the top of this
log), and the Rust verdict is now meaning-based (sweep + check + linear).
