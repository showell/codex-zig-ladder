# Outbound: backlog rows for findings 37 and 38

TEMPORARY -- delete in the commit that records them as sent, the way
PR-HEAP-UNIFICATION.md was. Until then this is the draft of the only
route these findings have upstream (contrib/README.md: a small branch
off master with a `Ladder:` line, entry in the quire's backlog).

---

| COMPILER-NN | **A self tail call in a definition whose return type is a FUNCTION jumps through a poison code pointer** | From Steve Howell's zig-plug ladder, 2026-08-24 (`findings/README.md` 38, reproducer `findings/probe-closure-return.codex`). Six lines, and the control is in the same file and the same run: `make-adder : Integer -> (Integer, Integer -> Integer)` defined as `make-adder (n) = add3 n` returns the closure and `((make-adder 5) 20) 22` answers **47**; `count-down (n) = if n == 0 then add3 5 else count-down (n - 1)`, identical return type, reaching the same closure through a **self tail call**, faults: `!EXC=0d RIP=a5f000ff53f000ff CR2=a5f000ff53f000ff`, with `applied 47` already printed on the line above. `a5f000ff53f000ff` is a poison fill, so the program jumped through a code pointer nothing wrote; there is no diagnostic, only the register dump. **The 2x2 is what makes it sharp:** Integer return reached directly (fine everywhere), Integer return reached by self tail call (fine -- the ladder's tier 13 exercises five such shapes byte-identically), function return reached directly (fine, 47), function return reached by self tail call (this row). Neither ingredient is enough alone. **Hypothesis, not measured:** self tail calls have been jumps since Update 30 (`X86_64.codex:75` `is-self-call`, `st-set-tail-pos`), which reuses the caller's frame; a partial application whose environment lives in that frame would be clobbered by the jump meant to return it. A fix has to read `emit-expr`'s tail-position path against wherever `build-partial-app`'s environment is allocated -- the ladder did not measure the emitter, only the fault. **Why nothing here catches it:** every `*-loop` in the compiler returns a value rather than a function, so the compiler cannot reproduce it on itself, and `plug-oracle-arith.codex`'s Lambdas section (added at Update 48 for COMPILER-13) exercises capture and application but no self-recursive definition that RETURNS a closure. Adding one row of that shape to that section would pin it. Seed-affecting if the fix is in the emitter. |

| COMPILER-NN | **The two top-level scans are three-function mutual TAIL recursion, so every plug and bare metal pay a frame per definition** | From the zig-plug ladder, 2026-08-24 (`findings/README.md` 37; instrument `stack_probe.py`). `scan-top-level` / `try-scan-type-def` / `try-scan-def-header` (`Syntax/Parser.codex`, streaming header scan) and `parse-top-level` / `try-top-level-type-def` / `try-top-level-def` (the parse) each go once around per top-level definition, and **every edge is a tail call** -- no frame in either cycle is live when the next is entered. Measured on the compiler's own back-end unit bundled as one document (2,503,544 bytes, 4,511 top-level definitions), compiled by a native binary built through the zig plug: 2,393 nested `scan_top_level` frames at the shallower cliff, 3,385 nested `parse_top_level` at the deeper one. **Nothing in the fleet flattens mutual tail calls** -- `is-self-call` is self-only here, in the python plug, and in the zig plug -- so this is paid on x86-64 as well, where it is invisible only because the kernel stack is generous and frames are small. **The change is source-shaped and needs no emitter work anywhere:** have the two `try-*` functions RETURN what they found (a small record: the accumulated lists plus the new state) instead of tail-calling the loop, and the loop tail-calls ITSELF, which every existing TCO already flattens. **Measured with exactly that change applied** (ladder branch `parser-scan-self-recursive`): the minimum thread stack the transpiled compiler needs for that document falls from 32 MB to **4 MB**, and what remains on the failing trace is `desugar_expr_at` at 297 frames -- structural recursion over one expression's nesting depth, not growth in the size of the document. Behaviour preserved: the restructured parser compiled that unit to IR, through the plug to a native binary, and both of the ladder's rung outputs are byte-identical to its Update 49 bank. **One detail a re-implementer must keep:** when `parse-type-def` fails, the definition parse re-reads from the ORIGINAL state, not from the state the type parser returned -- the two functions this replaces did that, and merging them makes it easy to lose. |
