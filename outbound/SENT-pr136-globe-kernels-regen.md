*Written by Claude, working with Steve Howell, on his account and at his
direction.*

## What this is

`apps/globe/kernels/GlobeKernels.wgsl` is stale. It was last generated at
Update 55, before the emitter fix in PR 129 that stopped the wgsl plug passing
`ptr<storage>` parameters and made helpers name the storage global directly.
PR 129 landed at Update 56 and its regeneration covered `apps/gpushow`, but
`apps/globe` was not regenerated, so this file still carries the old shape.

naga (Firefox's own front end) rejects it as it stands at Update 57:

```
error: Function 'earth_shade_pixel' is invalid
  = Argument 'framebuf' at index 0 is a pointer of space Storage, which can't be passed into functions.
```

Chrome's Tint accepts it, so the demo renders there and fails in Firefox --
the expected direction, and the only naga-failing kernel left in the tree.

## The change

One generated file, regenerated by `codex/plugs/wgsl` from
`apps/globe/kernels/GlobeKernels.codex` on this checkout. No source changes.
The helpers now name `earth_pixel_framebuf_buf` / `earth_pixel_tex_buf` /
`bh_pixel_framebuf_buf` directly, and naga validates the whole kernel; with it,
every `.wgsl` in the tree passes.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01YMgqDFdVkFRPR6auf3zfTT
