# Update 54 ceremony log

Upstream `c689cafb` + correction `14ec571b`, seed `FCBABF07479516DE`
(3,116,369 bytes). Pin branch `u54-rebank`.

What was changed and what was run, in order, one line each. Detail lives in
the commit it points to; findings go to the register, measurements to
JUSTIFICATIONS.md. This file is the thread, not the record.

## Steps

- **Read before running anything.** Fetched U54. 250 files, +31,593/-8,357
  over U53. `seed_identity` derived Update 54 with no matcher change -- the
  release note's seed line matched the fourth form taught at U53.

- **NINE of our TEN PRs landed; PR 100 did NOT.** Verified by content, not by
  their word. The nine: 99, 101, 103, 105, 107, 108, 111, 112, 114.
  - PR 114 landed VERBATIM -- `entry`, `stop > entry`, the early return gone;
    the only difference is `__linked-list-push` -> `lex-errors-push`, their
    own memory-campaign rename.
  - PR 101's code landed but NOT as sent: the witness is computed only when
    the context's element type carries type variables, so a fully typed list
    literal lowers exactly as before. Ours lowered element 0 on every
    non-empty literal -- roughly 2,900 extra lowerings per self-compile, and
    no GC to reclaim them. Their change, their measurement.
  - PR 105 landed UN-STACKED from PR 100. Our `zig-plug-real-bitcast` carries
    PR 100 as an ancestor; theirs does not. u54's `ZigEmitter` has
    `cx_real_to_bits` / `cx_bits_to_real` and neither `cx_real_to_int` nor
    `cx_real_from_int`.
  - **PR 100's blocker is the FIXTURE, not the emitter.** Upstream checked the
    guards against the code and found no input where the zig arm disagrees
    with x86. `codex/test/ops` is cross-graded on arm64 and riscv64, and both
    saturate where x86 answers the integer indefinite. Row moved to plugs
    2.19 (2.06-2.18 are taken upstream, several twice).
  - **The PR paid for itself unlanded**: reviewing it found `rv-fcvt-l-d`
    passing `rs2 = 0`, selecting the 32-bit FCVT.W.D, so riscv `real-to-int`
    and `show` on a Real were silently wrong for EVERY value above 2^31.
    Fixed upstream as plugs row 1.100.

- **The pin moved to `u54-rebank`, stack `u54-plus-pr100`.** The stack is PR
  100 rebased onto the release plus one commit answering its review: `run.ps1`
  now passes `-Kernel` on both arms (the test was absolute, `compile.ps1`'s
  default is relative, so from any cwd but the repo root the check passed and
  the compile failed one line later), and the backlog row is renumbered and
  its status corrected.
  - **"This box has no pwsh" was wrong, and it was wrong about a tool this
    repository calls every run.** pwsh 7.5.4 is at `~/.local/pwsh/pwsh`, off
    `PATH`, which is exactly the spelling `ast/oracle_lib.sh:159` uses to run
    every bundler. `run.ps1` parses clean and the selection was then driven
    from a cwd OUTSIDE the repository -- the defect's own case -- with the
    self-hosted kernel present and absent in turn; both arms answer an
    absolute path and both pass `-Kernel`. Corrected on the branch in
    `a2556393`. Still not run: the plug end to end, which needs a guest.

- **The stale zig plug was moved OUT of the shared checkout** to
  `~/runs/stale-zig-plug-2026-08-26-a961dcb6/`. It is gitignored, so no branch
  switch touches it, and four ladder scripts resolve it by path; left in place
  the pin move would have put a u49-era plug binary under a u54 tree with
  nothing announcing it. `check_paths.py` now fails loudly on the missing file
  and names its own remedy, which is the correct state.

- **After the pin move:** `check_harness_gates.py` green (6 emit roots, 7 bags
  merged, both harnesses gated). `check_paths.py` 6/6 tracked, 6/6 ladder, 0
  unconverted derivations, 0/2 built -- run `cycle.sh` once. The banked-against
  table stays at Update 53 until the new bank lands; the WARN says so itself.

## THE DECK INTRINSIC: why no rung that lexes would run, and it was ours

The first rung died in 5 s and the answer took most of the morning and two
wrong turns. Written out because the mechanism is five links long and every
one of them is correct on its own.

- **The fault.** `lex` and later `desugar` both page-faulted at
  `tokenize-collect+546` -- same function, same offset, stack
  `opening -> tokenize -> tokenize-collect`, resolved against each subject's
  own `.cdx.map`. `!EXC=0e`, CR2 a wild `0x2_0000000f`.
- **What changed.** `Syntax/Lexer.codex` gained `lex-state-below`: 0 uses at
  U53, 4 at U54. `tokenize-collect` now rewinds the bivy after every token and
  is correct only if `make-token`'s record was really moved to the deck first.
- **Why it was not.** `deck-record` is the IDENTITY in source -- in
  `Core/PhaseAllocator.codex` byte-for-byte as in our stub. The real behaviour
  is a call-site transform, `emit-deck-record-wrapper`, gated on
  `deck-record-intrinsic`, and the gate asks whether `init-phase-allocator` is
  among the IR DEFS (`X86_64Chapter.codex:1264`).
- **Why the def was missing.** Our harness openings never CALLED it. The real
  driver's `compile-lex` opens with `init-phase-allocator`, then `build
  lex-deck-height`, then passes a ceiling into `tokenize-into`. Ours called
  bare `tokenize`. With no caller the def is unreachable and
  `ir-prune-unreachable-roots` (`opening.codex:1359`, the seed's ordinary CDX
  path) removed it -- so the gate read "no phase allocator" and every
  `deck-record` compiled to `mov rax,rdi ; ret`.
- **CARRYING THE CHAPTER IS NOT ENOUGH, and that is the sharp part.** `desugar`
  bundles `Core/PhaseAllocator.codex` and faulted identically. A bundle can
  satisfy every cite, carry the right chapter, and still be compiled without
  the mechanism, because the gate's input is something dead-code elimination
  can take away.

**THE LADDER ALREADY KNEW, IN FOUR PLACES.** `lower`, `ir_to_codex`,
`ir_to_wire` and `ir_to_codex_roundtrip` call `DECK_PROLOGUE`, and the comment
above each says why in the words this incident would have used: "Without it
deck-record compiles to `mov rax,rdi ; ret` and the deck discipline is off for
the whole unit, **which is the condition that faulted clamp**." Four bytes,
which is exactly what `deck-record` measured as in both faulting maps. The fix
reached four harnesses of twelve and stopped. This is the second time in one
day -- `emit_harness.py` records `ir-emit-roots` drifting from six to four in
BOTH hosted harnesses at once, where "being wrong together looks exactly like
being right."

**Confirmed by one variable before being applied.** desugar, prologue only,
bundle untouched: 923-byte fault dump -> 6,041-byte truth, `lex-tokens 5727`,
`lex-errors 0`. Presence check: `init-phase-allocator` ABSENT from the old
symbol map, at `0x0010B8FE` in the new one, and the subject grew 438,050 ->
525,673 bytes, which is the `__deck-enter`/`__deck-exit` pairs the gate flip
emits.

**Scope, deliberately narrow.** Five rungs call `tokenize` -- lex, parse,
desugar, scope, check. `ir_to_x86`, `lir_to_x86` and `passes_to_x86` consume IR
and never lex, so they are untouched. lex and parse also gained the chapter and
`BootPaintStubs`; `LexStubs` then gave up `deck-record` and `deck-short-of`,
and parse dropped `LexStubs` entirely once CDX3006 caught the duplicate
`copy-sx-diag` against the real one in `SyntaxNodes`.

**Two wrong turns, recorded because the pattern outlives the bug.** First:
"our stub is wrong" -- killed by desugar, which carries the real chapter and
crashed identically. Second: "the guest never started" -- it started, faulted
in a fraction of a second, and sat with its dump in a buffer nobody was
reading; zero CPU meant crashed immediately, not never ran. Both readings ran
in the direction of a theory already formed. The essay is
`notes/the-harness-that-skipped-the-setup.md`.

## WHERE THIS STOPS, 2026-09-02

**Five rungs bank clean at U54: lex, parse, desugar, scope, check.** Timings
all slightly under U53 (lex 15s/17s, parse 45/56, desugar 57/64, scope 68/71,
check 126/129). The sixth, `lower`, faults and the run stops there.

**`lower` faults `!EXC=0d` at `__str_concat+72`, and it is NOT the deck
ceiling.** Two runs either side of that fix are byte-identical in outcome --
same 65-line file, same 1,660 bytes, R13 identical to the bit
(`f883480824448b4c`, which decodes as x86 instruction bytes: a register holding
code), only heap addresses moving. The commit that added the ceiling is right
about zero-ceilings disabling the guard and WRONG that it fixed this. The truth
ends at `type-bindings 3`, so it dies inside `lower-chapter`.

**THE FAULT GATE EARNED ITS KEEP TWICE**, and the second was the harder case:
`lower.truth` was 1,660 bytes with 46 lines of correct output and the dump
appended, which reads like a truth. Stamping refused, the five banked rungs
stand, and the run stopped rather than building on a bad foundation.

**Two candidate causes, neither established:**
- COMPILER-38's rename pass, new at U54, which builds `v_1` binder names by
  string concatenation -- the very helper the fault lands in;
- the deck reservations. I used the RAW floors (scale 100); the driver derives
  a scale from unit length, and for lower's 1,166,918-byte unit that is 77.
  1,232 MB reserved against the driver's 872 MB.

**AGREED PLAN, in this order (Steve, 2026-09-02):** isolate first -- run
`lower` alone with `rename = False`, one guest and one variable, which settles
whether the rename pass is implicated. THEN correct the scaling, which is right
on principle whatever the answer. Doing the scale fix first cannot distinguish
"fixed it" from "moved it".

See `HARNESS_FIDELITY.md` for the standing account: the harness reimplements
`opening.codex`, and four of the five deviations found today are the same
mistake -- calling the driver's functions without doing the driver's setup.

## Two defects found while chasing it

- **`run_cdx` ignores the host's guest-size cap.** `codex_vm.py`'s header says
  a host caps EVERY driver by exporting `CODEX_MEM_MB` "instead of trusting
  each wrapper to pass a number explicitly" -- and `run_cdx(kernel,
  mem_mb=1024, ...)` defaults to a NUMBER, so `MEM_MB` is never consulted on
  the run path. The droplet exports 3072 and every run-stage guest gets 1024.
  `compile_blob` takes `None` and is correct. NOT today's cause; not fixed.
- **A silent guest costs ten minutes.** `recv_all` breaks only on socket
  timeout or EOF; there is no halt detection, and the truth arm passes
  `idle_timeout=600`. A guest that faults early then sits quiet is waited out
  in full. This is what made both faults look like hangs.

## What the bank costs at this Update

- `~/golds/53b3b2137644/` is DEAD as a comparand: cut at master-plus-outbound
  with seed `B066CEB5`, and every stage the Rust front end has an oracle for
  moved -- Lexer +122, Parser +97, Desugarer +42, TypeChecker +324, Unifier
  +94, Lowering +260. Real literals and the Real printer are the reason.
- The 14 rung truths under `u53-14of14` need re-cutting; the seed moved.

## Owed to Damian from the comment thread

- **Issue 97, and they asked directly.** COMPILER-34 closed at their 20888 by
  a route other than the hoist: the desugarer now derives `Eq` for any
  self-recursive sum, so no `IrEq` on such a type reaches any backend. They ask
  us to confirm on our ladder that `recursive-eq` moves `differ` -> `match`
  with no change on our side -- "the independent arm we do not have, and worth
  more than our three." `recursive-eq.no-cross` is deleted in u54. This falls
  out of the corpus run for free.
- **PR 103 left plugs row 2.02 OPEN**, and the open half is ours to know
  about: our claim that a payload-carrying constructor is never a duplicate is
  wrong. `emit-zig-match-arm` emits a BARE prong when the binders are all
  unused (`:2155` one sub, `:2158` several), so two such arms still collide and
  still refuse at zig. `zig-pat-switch-value` only answers for
  `list-length subs == 0`.
- **PR 101's house rule, for every PR after this one:** column-2 prose is
  reserved for external formats, magic numbers and performance
  characteristics. Roughly half our added lines were prose explaining their own
  code, and they dropped it.
