Chapter: WarmupFib

Section: Main

  fib : Integer -> Integer
  fib (n) =
   if n < 2 then n
   else fib (n - 1) + fib (n - 2)

  opening : [Console] Nothing
  opening = act
   print-line-uni (show (fib 10))
   print-line-uni (show (fib 15))
  end
