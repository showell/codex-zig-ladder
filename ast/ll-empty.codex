Chapter: LlEmpty

  empty-out : Integer -> List Integer
  empty-out (n) = __linked-list-to-list []

  opening : [Console] Nothing = act
    let xs = empty-out 1
    in print-line-uni ("len " & show (list-length xs))
  end
